#!/bin/bash

if [[ "$OSTYPE" == "darwin"* ]]; then
  # Special support for OS X
	TMPDIR=/private$TMPDIR docker compose -p cobalt-api -f localstack/docker-compose.yml up
else
	docker compose -p cobalt-api -f localstack/docker-compose.yml up
fi